---
apiVersion: v1
kind: ConfigMap
metadata:
  name: application-overview-dashboard
  labels:
    grafana_dashboard: "1"
    grafana_folder: "Application Monitoring"
data:
  application-overview.json: |
{{ lookup('file', role_path + '/templates/grafana-dashboards/application-overview.json') | indent(4, True) }}